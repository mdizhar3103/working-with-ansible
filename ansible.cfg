[defaults]
inventory = inventory
remote_user = izhar
ask_pass = false
host_key_checking = true 
roles_path = ./roles

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
